import { describe, it } from "vitest"

describe("ConfirmDialog async contract", () => {
  it.todo("stays open and disables actions while confirmation is pending")
  it.todo("closes after confirmation succeeds")
  it.todo("remains open and recoverable when confirmation rejects")
})
