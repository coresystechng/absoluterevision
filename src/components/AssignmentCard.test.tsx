import { describe, it } from "vitest"

describe("AssignmentCard interaction contract", () => {
  it.todo("exposes one semantic assignment link and a separate More actions button")
  it.todo("never navigates when the More actions menu is activated")
})
