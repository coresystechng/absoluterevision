# Implementation Plans

Generated by the `improve` skill on 2026-07-11 against commit `d274e39`.
Execute these plans in order. Each executor must read its plan completely,
honor its STOP conditions, run every verification gate, and update the status
row when finished.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish the automated test baseline | P1 | M | — | DONE |
| 002 | Authenticate serverless API requests with verified Neon Auth JWTs | P1 | M | 001 | DONE |
| 003 | Move application database access behind authenticated API routes | P1 | L | 001, 002 | TODO |
| 004 | Make related database mutations atomic | P2 | M | 003 | TODO |
| 005 | Make the dashboard an attention-first operational workspace | P1 | L | 001 | DONE (verified at `e128398`) |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED` (with a one-line
reason), or `REJECTED` (with a one-line rationale).

## Dependency notes

- Plan 001 lands first so the security migrations have a repeatable regression
  suite and a single `npm test` gate.
- Plan 002 establishes trustworthy server-side identity before Plan 003 adds
  profile, team, and assignment API routes.
- Plan 003 removes browser-held PostgreSQL credentials and centralizes writes
  on the server. Plan 004 then adds transactions at that stable boundary.
- Plan 005 is independent of Plans 003 and 004. It depends only on the test
  baseline and can execute against the current UI while preserving the
  operator's existing uncommitted dashboard and team-management work.

## Findings considered and rejected

- `npm audit --omit=dev` reports Better Auth advisories through the beta Neon
  Auth dependency. The highest-severity advisories concern OAuth/OIDC/provider
  plugins whose reachability is not established in this email/password app.
  Reassess during a deliberate Neon Auth SDK upgrade; do not perform a blind
  major-version update as part of these plans.
- A full performance, accessibility, documentation, and product-direction
  audit was outside the requested quick-audit scope.
